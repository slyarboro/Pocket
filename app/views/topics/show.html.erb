<br />  <div class="col-md-12">
<% if current_user %>
<%= link_to "Return to All Topics", topics_path, class: "btn btn-info" %>
  <center>
      <h1><%= @topic.title %></h1>
      <% if policy(@topic).edit? %>
      <%= link_to edit_topic_path do %>
      <span class = "glyphicon glyphicon-pencil"></span>
      <% end %>
      <% end %>

      <% if policy(@topic).destroy? %>
      <%= link_to @topic, method: :delete, data: { confirm: 'You have requested to delete this topic.
      Please click OK to confirm.'} do %>
      <span class= "glyphicon glyphicon-trash"></span>
      <% end %>
      <% end %>
  </center>

  <div class="col-md-12">
    <br /><br /><br />
  </div>

  <h3>Bookmarks &nbsp;<%= link_to "New Bookmark", new_topic_bookmark_path(@topic), class: 'btn btn-primary' %></h3>
  <div class="row">
    <div class="col-md-10">
      <div class="media">
        <div class="media-body">
          <table class="table table-hover">
            <tbody>
              <tr class='js-items'><p>
                <%= render @bookmarks %>

                <!-- <% @topic.bookmarks.each do |link| %> -->
                <!-- <%= render partial: 'likes/like', locals: { bookmark: link } %> -->
                <!-- <%= link_to "#{link.url}", topic_bookmark_path(link.topic, link) %> -->
                <!-- <% end %> -->
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
  <!-- <% end %> -->
</div>
