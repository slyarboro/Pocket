<br />  <div class="col-md-12">
<% if current_user %>
<%= link_to "Return to All Topics", topics_path, class: "btn btn-info" %>
  <center>
      <h1><%= @topic.title %></h1>
      <%= link_to "Edit Topic", edit_topic_path, class: 'btn btn-warning' %>
      <%= link_to "Delete Topic", @topic, method: :delete, class: 'btn btn-danger', data: { confirm: 'You have requested to delete this topic.
        Please click OK to confirm.' } %>
    <!-- </div> -->
  <% end %>
  </center>

  <!-- <div class="col-md-12"> -->
    <br /><br /><br />
  </div>

  <h3>Bookmarks &nbsp;<%= link_to "New Bookmark", new_topic_bookmark_path(@topic), class: 'btn btn-primary' %></h3>
  <div class="row">
    <div class="col-md-10">
      <div class="media">
        <div class="media-body">
          <table class="table table-hover">
            <tbody>
              <tr class='js-items'><p>
                <%= render @bookmarks %>
              </tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
